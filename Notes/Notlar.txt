--> Queue oluştururken

#include <queue>

queue<int> integers;
integers.push(5);
integers.pop();
printf("Size : %d\n",integers.size());
printf("Front : %d\n",integers.front());
printf("Back : %d\n",integers.back());


--> Random sayı oluştururken

#include <ctime>

srand(time(NULL));
int number, n = 100;
number = rand() % n; --> range 0-n oluyor


--> Thread 

#include <pthread.h>

int a = 5;
pthread_t th1;
pthread_create(&th1, NULL, funThreadSingleParam,(void *)&a);
pthread_join(th1, NULL); --> bitmesini beklemek için


sleep(3) 	--> 3 saniye bekle

pthread_mutex_t mutexFile;		--> Kilitli çalışması gereken dosya
pthread_mutex_lock(&mutexFile);	--> Kilitliyor
pthread_mutex_unlock(&mutexFile); --> Kilidi açıyor

// Örnek Kullanım
		pthread_mutex_lock(&mutexFile);
        FILE *file = fopen(fileName,"a+");
        fprintf(file,"increase counter: %d\n",counter);
        fclose(file);
        printf("Thread increase wrote...\n");
        pthread_mutex_unlock(&mutexFile);


--> Window oluşturma

initscr();	--> Ekranı oluşturuyor
start_color(); --> Renk kullanmayı açar
keypad(stdscr, true); --> 